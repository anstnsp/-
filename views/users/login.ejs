<!-- views/users/new.ejs -->

<!DOCTYPE html>
<html>
<head>
    <% include ../partials/header %>
</head>
<body>

<div class="container">
    <h3>로그인</h3>
    <!-- <form action="111" method="post"> -->
        <div class="form-group">
            <label class="control-label" for="id">아이디:</label>
            <input class="form-control" type="text" id="username" name="username"/>
        </div>
        <div class="form-group">
            <label class="control-label" for="pwd">비밀번호:</label>
            <input class="form-control" type="password" id="password" name="password"/>
        </div>
        <button class="btn btn-success"  id="submitBtn" >로그인</button>
        <!--<button class="btn btn-danger" type="button" onclick='location.href="/"'>취소</button>-->
        <button class="btn btn-danger" type="button" onclick=window.close()>취소</button>
    <!-- </form> -->
</div>
</body>
</html>

<script>
    $("#submitBtn").click((data) => {
       
        let username = $("#username").val();
        $.ajax({
            url: "/auth/login",
            method: "POST",
            data: {"username":$("#username").val(), "password":$("#password").val()},
            success: function(result) {
                alert(username+"님 환영합니다!!")
                //opener.parent.location.reload();
                opener.location.replace("http://211.232.21.12:7777/");

                window.close();

            }, 
            error : function(result) {
                alert("입력하신 아이디와 비밀번호가 일치하지 않습니다.");
            }

        });
        return true;
    })
</script>

